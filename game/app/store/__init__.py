"""서버 저장소 — 계정·티켓·메타·런 (docs/설계/1_통합시스템설계 §5).

**시뮬레이션 코어는 이 패키지를 모른다.** 코어는 의존성이 0개인 순수 계산이고, 여기는
그것을 부르는 쪽의 인프라다. 의존 방향이 뒤집히면 헤드리스 러너가 DB 없이 못 돈다.

이 패키지의 무작위성은 R5 의 적용 대상이 아니다. 토큰과 시드 발급에 `secrets` 를 쓰는데,
그것은 **게임 난수가 아니라 예측 불가능해야 하는 값**이다. 코어 안에서 `secrets` 를 쓰면
R5 위반이지만, 시드를 만들어 코어에 넣어 주는 것은 그 반대다 — 시드가 예측 가능하면
유리한 판을 골라 담을 수 있다 (docs/설계/7_변조방지 T2).
"""
