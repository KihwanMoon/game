"""I/O 계약 — 블록 목록·룸 템플릿·규칙표의 직렬화 형식 (TDD §3).

이 패키지는 Phase 3 에서 TypeScript 로 그대로 이식된다. 파이썬 프로토타입 전체가
버릴 코드인 반면(TDD §1.2) 여기의 스키마와 밸런스 JSON 만 살아남으므로, 언어 중립적인
형태를 유지한다 — 파이썬에만 있는 표현에 기대지 않는다.

정본은 `game/resources/` 의 JSON 이고 여기 있는 것은 그것을 읽는 타입이다.
값을 코드에 적지 않는다.
"""
