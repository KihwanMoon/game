"""검증 서버 (B단계). 표준 §12 의 `api/` 자리다.

지금까지 이 디렉터리를 두지 않은 이유는 서버가 없었기 때문이고, 이제 생겼다.

**여기는 얇아야 한다.** HTTP 를 도메인으로 옮기는 일만 하고, 판정은 `app/services/` 가,
저장은 `app/store/` 가 한다. 규칙이 라우트 안으로 들어오면 그것을 헤드리스로 검증할 수
없게 된다.
"""
