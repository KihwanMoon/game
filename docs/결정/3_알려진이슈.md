# 알려진 이슈

> 우리가 만든 결함이 아니라 **밖에서 온 것과 아직 못 지킨 것**의 목록이다.
> 정해야 할 것은 `결정/1_결정대기목록`, 안 만든 것은 `결정/2_기획공백과_잔여작업` 에 있다.

## 1. 사내 표준 문서의 결함 — 정본 개정 대기

표준 문서 자체의 결함이다. 저장소에서 규칙을 완화한 것이 아니라 **게이트가 아예 돌지
않거나 돌고도 거짓을 말하는 상태**를 푼 것이며, 정본이 `RULESET_VERSION 15` 로 개정되면
로컬 수정을 지우고 재동기화한다.

사유는 `.claude/conventions.lock`, 근거와 제안 diff 는 `참고/3_표준문서_개정제안` 에 있다.

표준 문서 자체의 결함이다. 저장소에서 규칙을 완화한 것이 아니라 게이트가 아예 돌지
않거나 돌고도 거짓을 말하는 상태를 푼 것이며, 정본이 `RULESET_VERSION 15` 로 개정되면
로컬 수정을 지우고 재동기화한다. 사유는 `.claude/conventions.lock`, 근거와 제안 diff 는
**`docs/참고/3_표준문서_개정제안.md`** 에 있다.

| # | 절 | 내용 | 상태 |
|:-:|:--|:--|:--|
| D1 | §8.5 | `check_all.sh` 가 `python` 을 부른다 (배포판 다수는 `python3` 만 제공) | 로컬 수정함 |
| D2 | §8.5 | 같은 단계가 `pipefail` 아래라 "검사 대상 0건"이 게이트 실패가 된다 | 로컬 수정함 |
| D3 | §8.3·§8.5 | 예외 정규식 `/tests/` 가 저장소 루트 `tests/` 를 놓친다 | 로컬 수정함 |
| D4 | §8.3+§8.5.3 | `pre-commit install` 과 `core.hooksPath` 가 충돌한다 | 훅 직접 배치로 해소 |
| D5 | §8.9 | `BANNED` 목록이 정본 PDF 에서 잘렸다 | **미해결 — 원문 확인 필요** |
| D6 | §2.1 | 동사 목록에 `move` 가 없다 — 행동 11종이 전부 `apply_` 로 시작한다 | 우회 중 |
| D7 | §4·§7 | **모듈 400줄을 아무것도 검사하지 않았다** (§7 표에 행 자체가 없다) | 게이트에 추가함 |
| D8 | §7 | 「함수 길이 = 완전 자동」이 사실이 아니다 (PLR0915 는 문장 수를 잰다) | **미해결 — 존량 10건** |
| D9 | §8.5 | `step()` 이 `shift` 뒤 `$1` 을 보고해 실패한 단계 이름이 뭉개진다 | 미해결 |
| D10 | §8.5.2×§8.9 | CI 가 mypy 를 설치하지 않는다 — §8.9 를 도입하면 127 로 죽는다 | 해당 없음(pre-push) |
| D11 | §8.9 | 선택 검사 도입 시 「완전 자동」 승격 범위가 과장돼 있다 | 미해결 |
| D12 | §1·§8.7 | 같은 예외 경로가 다섯 문법으로 흩어져 있다 (훅 것은 상대경로에서 안 걸린다) | 미해결 |
| D13 | §10.2 | `apply-conventions.md` 자체가 배포 목록에 없다 — 재동기화 경로가 끊긴다 | 미해결 |
| D14 | §3·§7 | 매직 넘버·매직 스트링에 강제 수단 행이 없다 | 미해결 |
| D15 | §8.5 | 게이트가 추적되지 않은 파일을 보지 않는다 | 미해결 |
| D16 | §10.3 | RULESET_VERSION 드리프트 대조를 아무것도 돌리지 않는다 | 미해결 |
| D17 | §7.1×§8.5.2 | 도구 버전 핀이 `ruff` 하나뿐이다 | 미해결 |

**D7 로 게이트가 한 단계 늘었다.** `tools/check_module_length.py` 가 §4 의 모듈 400줄
상한을 본다. 테스트도 대상이다 — §1 이 테스트에 면제하는 것은 독스트링·타입 힌트·함수명
셋뿐이다. 켤 때 걸린 존량 11건은 §4 의 "책임이 둘 이상" 기준으로 분해해 0으로 만들었다.

**D15 가 실제로 물었다.** B단계에서 새 파일을 만들고 `check_all.sh` 를 돌렸을 때
「전부 통과」가 나왔지만, 파일이 아직 추적되지 않아 네이밍 검사가 그것을 보지 않은
것이었다. `git add` 뒤 pre-commit 훅이 잡았다 — 훅이 없었다면 허용되지 않은 동사가
그대로 들어갔다. **새 파일을 만든 뒤에는 `git add` 를 먼저 하고 게이트를 돌린다.**

## 2. 정본과 사본의 드리프트

사본을 고치면 안 되는데 고쳐진 것, 또는 정본이 따라오지 않은 것이다.

| # | 사본 | 정본 | 상태 |
|:-:|:--|:--|:--|
| X1 | `design/tokens/spacing.css` 의 반응형 토큰 | Claude Design 프로젝트 | **사본이 앞서 있다.** 역이관 필요 (`결정/1_결정대기목록` #44) |
| X2 | `ruff.toml`·`tools/`·`.claude/rules/` | Confluence `python-coding-standard` v14 | 개정 제안 17건 미반영 (#32) |
| X4 | `GlyphState` 의 `blocked` 상태와 해칭 표기 | Claude Design 프로젝트 | **사본이 앞서 있다.** 역이관 필요 |
| X5 | 서랍·캐릭터 시트·관리자 화면, 레이아웃 토큰 4종 | Claude Design 프로젝트 | **사본이 앞서 있다.** 역이관 필요 (2026-08-30) |
| X3 | GDD §1 플랫폼 줄 · 로드맵 §2 블록 수 | — | 실제와 어긋난다 (블록은 v4 로 18/14/9) |

## 3. 검증되지 않은 것

게이트를 통과했다고 해서 옳다는 뜻이 아닌 것들이다.

| # | 항목 | 내용 |
|:-:|:--|:--|
| Y1 | **R2 전략 다양성** | 12전략 중 3개만 클리어(kite 100% · door_hold 87% · spring_camp 47%). 해소가 아니라 **유예**다 (`참고/2_전략공간실측`) |
| Y2 | **G1 재미 판정** | 미판정. 로드맵이 실패 시 **재설계 또는 중단**을 명시했다 |
| Y3 | **G2 시뮬레이션 정확성** | 미판정 |
| Y4 | CPU 예산 vs 슬롯 트레이드오프 | 현재 둘이 함께 포화해 트레이드오프가 **작동하지 않는다** |
| Y5 | 관전 지속 시간 | 오토배틀 관전이 견딜 만한지 측정한 적이 없다 |

## 4. 운영

| # | 항목 | 상태 |
|:-:|:--|:--|
| Z5 | 스키마 이행 도구 없음 | `entity_record` 통합이 첫 실제 이행이었고 명시적 SQL(`migrate.sql`)로 처리했다. **두 번째 이행이 이만큼 복잡하면 도구를 들인다** |
| Z0 | 로그인 살포 | 아이디 기준 제한은 붙였다(5회/10분). **아이디를 바꿔 가며 훑는 살포는 남아 있다** — 주소 기준 제한이 필요하고, 그것은 프록시 뒤 정상 사용자를 함께 막지 않게 설계해야 한다 |
| Z1 | 알림 채널 | 모니터링은 5분 주기로 돌지만 **실패해도 사람에게 가지 않는다** |
| Z2 | 이중화 | 없다. 단일 호스트·단일 컨테이너 |
| Z3 | 검사 이전의 시험 데이터 | 격리(`game_test`) 전에 돌린 검사가 만든 익명 계정 22개가 운영 DB 에 남아 있다. 데이터가 없는 빈 계정이라 무해하지만, **지우지 않는다** — 그 사이 실제 방문자가 만든 계정이 섞여 있을 수 있고 구분할 방법이 없다 |
| Z4 | `edge-proxy` 롤백 경로 | git 저장소가 아니다. `nginx.conf.bak.20260828-201825` 가 유일한 되돌리기 수단 — **지우면 안 된다** |
